<script id="search_result_template" type="text/x-handlebars-template">
	<table class="table table-hover">
      <thead>
        <tr>
          <th>Group Number</th>
          <th>Project Name</th>
          <th>Evaluator</th>
          <th>Role</th>
          <th>Type</th>
        </tr>
      </thead>

      <tbody>
      	{{#each comments}}
      	<tr>
      		<td>{{group}}</td>
      		<td><a class="tag">{{project}}</a></td>
          <td>{{first_name}} {{last_name}}</td>
          <td>Advisory Board</td>
      		<td>Comment</td>
      	</tr>
      	{{/each}}

      	{{#each grades}}
      	<tr>
      		<td>{{group}}</td>
      		<td><a class="tag">{{project}}</a></td>
          <td>{{first_name}} {{last_name}}</td>
          <td>Instructor</td>
      		<td>Grade</td>
      	</tr>
      	{{/each}}

      </tbody>
    </table>
</script>